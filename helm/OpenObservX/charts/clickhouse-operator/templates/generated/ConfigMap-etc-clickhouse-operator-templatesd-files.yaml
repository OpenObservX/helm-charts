# Template Parameters:
#
# NAME=etc-clickhouse-operator-templatesd-files
# NAMESPACE=kube-system
# COMMENT=
#
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ printf "%s-templatesd-files" (include "clickhouse-operator.fullname" .) }}
  namespace: {{ .Release.Namespace }}
  labels: {{ include "clickhouse-operator.labels" . | nindent 4 }}
data: {{ tpl (toYaml .Values.configs.templatesdFiles) . | nindent 2 }}
