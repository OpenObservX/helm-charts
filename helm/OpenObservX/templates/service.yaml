apiVersion: v1
kind: Service
metadata:
  name: {{ include "OpenObservX.fullname" . }}
  labels:
    {{- include "OpenObservX.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "OpenObservX.selectorLabels" . | nindent 4 }}
