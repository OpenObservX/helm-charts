apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "OpenObservX.fullname" . }}-test-connection"
  labels:
    {{- include "OpenObservX.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "OpenObservX.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
